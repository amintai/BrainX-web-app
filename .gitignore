# Dependencies
node_modules/

# Package manager lockfiles from tools we don't use
yarn.lock
package-lock.json

# Build artifacts
dist/
coverage/

# Env files (committed: .env.example only)
.env
.env.local

# Supabase local state
supabase/.temp

# Editor / OS
.DS_Store
