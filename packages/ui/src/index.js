// Placeholder JSX-capable export — components are added when the UI package is populated.
// No components are implemented yet (out of scope for boilerplate-setup); this named export
// exists so the package resolves and pnpm ls -r sees a valid entry point.
export const UI_PACKAGE_NAME = '@brainx/ui';
